export default function App() {
  return (
    <div
      style={{
        minHeight: "100vh",
        background: "linear-gradient(180deg, #0b1020 0%, #12192f 45%, #1a223d 100%)",
        color: "#ffffff",
        padding: "40px",
        fontFamily: "Inter, Segoe UI, Arial, sans-serif",
      }}
    >
      <div
        style={{
          maxWidth: "1100px",
          margin: "0 auto",
          background: "rgba(17, 24, 43, 0.9)",
          border: "1px solid rgba(255,255,255,0.08)",
          borderRadius: "24px",
          boxShadow: "0 26px 70px rgba(0,0,0,0.28)",
          padding: "32px",
        }}
      >
        <div
          style={{
            fontSize: "12px",
            fontWeight: 800,
            textTransform: "uppercase",
            letterSpacing: "0.4px",
            color: "#7dd3fc",
            marginBottom: "8px",
          }}
        >
          Stro Chievery
        </div>

        <h1
          style={{
            margin: 0,
            fontSize: "32px",
            fontWeight: 900,
            letterSpacing: "-0.8px",
          }}
        >
          Client boot test is working
        </h1>

        <p
          style={{
            marginTop: "12px",
            fontSize: "16px",
            lineHeight: 1.7,
            color: "#cad5ef",
          }}
        >
          If you can see this page, React is rendering correctly and the old problem is
          somewhere in the original app structure or imports.
        </p>

        <div
          style={{
            marginTop: "24px",
            display: "flex",
            gap: "12px",
            flexWrap: "wrap",
          }}
        >
          <button
            style={{
              minHeight: "42px",
              padding: "11px 14px",
              borderRadius: "14px",
              border: "none",
              background: "linear-gradient(180deg, #3b82f6, #2563eb)",
              color: "#fff",
              fontWeight: 800,
              cursor: "pointer",
            }}
          >
            Homepage
          </button>

          <button
            style={{
              minHeight: "42px",
              padding: "11px 14px",
              borderRadius: "14px",
              border: "1px solid rgba(255,255,255,0.08)",
              background: "rgba(255,255,255,0.08)",
              color: "#fff",
              fontWeight: 800,
              cursor: "pointer",
            }}
          >
            Test Button
          </button>
        </div>
      </div>
    </div>
  );
}